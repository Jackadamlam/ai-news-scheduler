name: Daily AI News

on:
  schedule:
    # 每天 7:00, 10:00, 13:00, 16:00, 19:00, 22:00 (北京时间)
    # UTC时间 = 北京时间 - 8小时
    - cron: '0 23 * * *'   # 北京时间 7:00
    - cron: '0 2 * * *'    # 北京时间 10:00
    - cron: '0 5 * * *'    # 北京时间 13:00
    - cron: '0 8 * * *'    # 北京时间 16:00
    - cron: '0 11 * * *'   # 北京时间 19:00
    - cron: '0 14 * * *'   # 北京时间 22:00
  workflow_dispatch:  # 允许手动触发

jobs:
  fetch-news:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    
    defaults:
      run:
        working-directory: Daily News
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.11'
        cache: 'pip'
        cache-dependency-path: 'Daily News/requirements.txt'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        
    - name: Create output directories
      run: |
        mkdir -p output logs data
        
    - name: Run news scheduler
      run: python main.py --once
      env:
        TZ: Asia/Shanghai
      
    - name: Commit and push changes
      run: |
        git config --local user.email "github-actions[bot]@users.noreply.github.com"
        git config --local user.name "GitHub Actions Bot"
        git add "Daily News/output/"
        if git diff --staged --quiet; then
          echo "No changes to commit"
        else
          git commit -m "🤖 Update AI news $(TZ=Asia/Shanghai date +'%Y-%m-%d %H:%M')"
          git push
        fi
